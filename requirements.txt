# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1

# Database
sqlalchemy==2.0.36
asyncpg==0.30.0
alembic==1.14.0

# Validation & settings
pydantic==2.10.3
pydantic-settings==2.6.1
email-validator==2.2.0

# Auth
PyJWT==2.10.1
bcrypt==5.0.0
python-multipart==0.0.18

# Task queue
celery==5.4.0
redis==5.2.1

# HTTP client (for AI engine, Shodan, NVD)
httpx==0.28.1

# DNS resolution (ownership verification)
dnspython==2.7.0

# Sync DB driver for Celery workers
psycopg2-binary==2.9.10

# Shodan API via httpx (no SDK needed — uses REST API directly)
